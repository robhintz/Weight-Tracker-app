<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    index page
    <ul>
    <% for (let i = 0 ; i < weightData.length ; i++) { %>
        <li>
            <p> <%= weightData[i].date %> </p>
            <p> <%= weightData[i].weight %> </p>
            <p> <%= weightData[i].comment %> </p>
        </li>
        <% } %>
    </ul>

    <a href = "/new">New</a>
</body>
</html>