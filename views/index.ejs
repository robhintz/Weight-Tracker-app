<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>weight tracker index</title>
    <header><%- include ('partials/header.ejs') %></header>
</head>
<body>
    <nav>
        <%- include ('partials/nav.ejs') %>
    </nav>

    <ul class="index">
    <% for (let i = 0 ; i < weightData.length ; i++) { %>
        <li>
            <p> <a href="/index/<%= weightData[i].id %>"><%= weightData[i].date %> </a> </p>
            <p> <%= weightData[i].weight %> </p>
        </li>
        <% } %>
    </ul>

</body>
</html>